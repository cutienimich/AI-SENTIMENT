import psycopg2

hostname = 'localhost'
database = 'demodb'
username = 'postgres'
password = '123456789'
port_id = 5432
conn = None
cur = None


try:
    conn =  psycopg2.connect(
        host = hostname,
        dbname = database,
        user = username,
        password = password,
        port = port_id
    )
    cur = conn.cursor()
    
    cur.execute('DROP TABLE IF EXISTS accounts')
    create_script = ''' CREATE TABLE IF NOT EXISTS accounts (
                                id SERIAL PRIMARY KEY,
                                email VARCHAR(255) UNIQUE NOT NULL,
                                password VARCHAR(255) NOT NULL,
                                created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
                            
    );
    '''
    
    cur.execute(create_script)
    
    insert_script = 'INSERT INTO accounts (email, password) VALUES (%s, %s)'
    insert_value = ("michellepostrado26@gmail.com", "hatdog123"), ("michelleriegopostrado@gmail.com", "5432")
    for record in insert_value:
        cur.execute(insert_script, record)
    
    conn.commit()
    

except Exception as error:
    print(error)

finally:
    if cur is not None:
        cur.close()
    if conn is not None:
        conn.close()
